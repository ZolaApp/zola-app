query getProject($projectSlug: String!) {
  project: getProject(projectSlug: $projectSlug) {
    id
    name
    slug

    missingTranslations
    newKeys
    translationKeys {
      key
      translationValues {
        value
      }
    }

    locales {
      name
      code
    }
  }
}
