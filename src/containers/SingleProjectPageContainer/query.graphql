query getProject($projectSlug: String!) {
  project: getProject(projectSlug: $projectSlug) {
    id
    name
    locales {
      name
      code
    }
    translationKeys {
      key
      translationValues {
        value
      }
    }
  }
}
