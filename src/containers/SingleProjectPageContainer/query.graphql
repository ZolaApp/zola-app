query getProject($projectSlug: String!) {
  project: getProject(projectSlug: $projectSlug) {
    id
    name
    slug

    missingTranslations
    newKeys
    translationKeys {
      id
      key
      translationValues {
        value
      }
    }
    locales {
      id
      name
      code
    }
  }
}
