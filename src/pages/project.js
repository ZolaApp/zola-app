// @flow
import SingleProjectPageContainer from '@containers/SingleProjectPageContainer'
import withAuthentication from '@higherOrders/withAuthentication'

export default withAuthentication(SingleProjectPageContainer)
